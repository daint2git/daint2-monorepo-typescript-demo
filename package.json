{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^14.0.27",
    "lerna": "^3.22.1",
    "typescript": "^3.9.7"
  },
  "scripts": {
    "build": "lerna run build",
    "publish": "lerna publish --conventional-commits"
  }
}
